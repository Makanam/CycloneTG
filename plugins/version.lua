do

function run(msg, matches)
  return 'CycloneTG Bot V5\nAn advanced Administration bot based on yagop/telegram-bot \n\sudo:Makan\nnew version.]]
end

return {
  patterns = {
    "^version$"
  }, 
  run = run 
}

end
